import mocha;
import { assert } from 'chai';

mocha.setup('bdd');

beforeEach(_ => {

});

afterEach(_ => {});


describe('send requests', _ => {
  it('should send get request', _ => {});

  it('should send post request', _ => {});

  it('should send patch request', _ => {});

  it('should send delete request', _ => {});

  it('should send put request', _ => {});
});

